# Ensure Windows batch scripts keep CRLF line endings (important for GitHub ZIP users)
*.bat text eol=crlf
*.cmd text eol=crlf

# (Optional) Keep PowerShell scripts consistent too
*.ps1 text eol=crlf
